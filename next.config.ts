import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Configuração compatível com Cloudflare Pages
  // Não utiliza API routes ou SSR incompatíveis
};

export default nextConfig;
